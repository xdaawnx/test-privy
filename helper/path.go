package helper

// TempDownload is a temporary file that will be deleted after sent
var TempDownload = "./asset/temp/download/"

// DirUploadedFile is a directory for saving upload file
var DirUploadedFile = "./asset/uploads/"

// DirTemplateFile is a directory for saving template file
var DirTemplateFile = "./asset/template/"

// Paths to containt all path in one array
var Paths = []string{TempDownload, DirUploadedFile}
